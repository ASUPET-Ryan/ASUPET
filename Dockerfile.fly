# Fly.io 部署 Dockerfile for ASUPET

# 使用 Nginx 作为基础镜像
FROM nginx:alpine

# 复制已构建的静态文件到 Nginx 默认目录
COPY dist/ /usr/share/nginx/html/

# 复制 Nginx 配置文件
COPY fly-nginx.conf /etc/nginx/nginx.conf

# 暴露端口 8080 (Fly.io 要求)
EXPOSE 8080

# 启动 Nginx
CMD ["nginx", "-g", "daemon off;"]
